session: blog

root: ~/workspace/git-blog-next-markdown

env:
  FOO: BAR

windows:
  - name: workspace
    root: ~/workspace/git-blog-next-markdown
    layout: main-vertical
    commands:
      - vi ./README.md

    panes:
      - type: horizontal
        root: .
        commands:
          - npm run dev
          - clear
      - type: horizontal
        root: .
